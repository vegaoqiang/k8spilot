apiVersion: apps/v1
kind: Deployment
metadata:
  name: video-cascader
  namespace: devops
  labels:
    app: video-cascader
status:
  replicas: 1
  updatedReplicas: 1
  readyReplicas: 1
  availableReplicas: 1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: video-cascader
  template:
    metadata:
      labels:
        app: video-cascader
    spec:
      volumes:
        - name: video-cascader-log
          emptyDir:
            sizeLimit: 1Gi
      containers:
        - name: video-cascader
          image: {{video_cascader}}
          ports:
            - name: debug
              containerPort: 10534
              protocol: TCP
            - name: dubbo
              containerPort: 10532
              protocol: TCP
            - name: other-8888
              containerPort: 8888
              protocol: TCP
            - name: web
              containerPort: 10531
              protocol: TCP
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
            - name: JVM
              value: '1024'
            - name: JVM_CUSTOM
              value: ' -Denv=TEST -Dfile.encoding=utf-8 -Dspring.cloud.nacos.config.file-extension=properties -Dspring.cloud.nacos.config.server-addr=nacos.public.svc.cluster.local:8848 -Dspring.cloud.nacos.config.username=nacos -Dspring.cloud.nacos.config.password=nacos -Dspring.cloud.nacos.discovery.server-addr=nacos.public.svc.cluster.local:8848 -Dspring.cloud.nacos.discovery.username=nacos -Dspring.cloud.nacos.discovery.password=nacos --add-opens=java.management/sun.management=ALL-UNNAMED --add-opens=java.base/sun.net=ALL-UNNAMED --add-opens=java.base/java.lang=ALL-UNNAMED --add-opens=java.base/java.math=ALL-UNNAMED --add-opens=java.base/jdk.internal.misc=ALL-UNNAMED -XX:+UnlockExperimentalVMOptions -XX:MaxRAM=1G -XX:InitialRAMPercentage=10 -XX:MinRAMPercentage=50 -XX:MaxRAMPercentage=70 -Xms512m -Xmx1024m -server -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:10534'
            - name: TZ
              value: Asia/Shanghai
            - name: APP_NAME
              value: video-cascader
            - name: LANG
              value: en_US.UTF-8
            - name: PROJECT_NAME
              value: video-cascader
            - name: WEBPORT
              value: '10531'
            - name: isolate
              value: video-sheng
            - name: LOG_KAFKA_REGION
              value: e30=
            - name: SENTINEL_URL
              value: '-'
            - name: CODE_VERSION
              value: '17'
            - name: DUBBO_ENV_KEYS
              value: zone,isolate
            - name: JVMMN
              value: '512'
            - name: ISKUBERAPP
              value: 'true'
            - name: CONFIG_ENV
              value: TEST
          resources:
            limits:
              cpu: '1'
              memory: 2Gi
            requests:
              cpu: 250m
              memory: 512Mi
          volumeMounts:
            - name: video-cascader-log
              mountPath: /home/docker/logs
          readinessProbe:
            httpGet:
              path: /hc.do?a=healthCheck
              port: 10531
              scheme: HTTP
            timeoutSeconds: 1
            periodSeconds: 5
            successThreshold: 1
            failureThreshold: 2
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
      imagePullSecrets:
        - name: regcred

---

apiVersion: v1
kind: Service
metadata:
  name: video-cascader-svc-cascade-node-port
  namespace: devops
spec:
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
      nodePort: 20533
  selector:
    app: video-cascader
  type: NodePort

---

apiVersion: v1
kind: Service
metadata:
  name: video-cascader-svc-nodeport
  namespace: devops
spec:
  selector:
    app: video-cascader
  ports:
    - protocol: TCP
      port: 10531
      targetPort: 10531
  type: ClusterIP